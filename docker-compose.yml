version: "3.9"

services:
  frontend:
    build:
      context: .
      target: runner
    image: spirits-frontend:latest
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
    networks:
      - spirits-network

networks:
  spirits-network:
    driver: bridge
